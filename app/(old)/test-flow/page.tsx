"use client";

export default function TestFlowPage() {
  return (
    <div className="mx-auto max-w-xl p-8">
      <h1 className="text-2xl font-semibold">Legacy Test Disabled</h1>
      <p className="mt-2 text-sm text-[color:var(--muted)]">
        The current flow is onboarding to backend auth session to dashboard.
      </p>
    </div>
  );
}
